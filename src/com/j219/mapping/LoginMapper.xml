<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.j219.dao.LoginDao">
    <resultMap type="com.j219.model.Login" id="Map">
        <id property="id" column="id" />
        <result property="username" column="username" />
        
    </resultMap>
    <select id="getUsernameByAccountAndpassword" parameterType="java.lang.String"
        resultMap="Map">
        select * from users where account=#{account} and password=#{password}
    </select>
    
</mapper>